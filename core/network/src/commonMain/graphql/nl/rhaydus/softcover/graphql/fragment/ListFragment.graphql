fragment ListFragment on lists {
    id
    name
    slug
    ranked
    privacy_setting_id
    updated_at

    list_books_aggregate {
        aggregate {
            count

            max {
                updated_at
            }
        }
    }

    list_books {
        ...ListBookFragment
    }
}
