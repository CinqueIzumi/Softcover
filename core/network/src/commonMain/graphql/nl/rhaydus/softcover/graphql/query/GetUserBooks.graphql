query GetUserBooks($statusIds: [Int!]) {
    me {
        user_books(
            where: { status_id: { _in: $statusIds } },
            order_by: { updated_at: desc }
        ) {
            ...UserBookFragment
        }
    }
}
