package nl.rhaydus.softcover.core.notification

/**
 * Desktop handle for the notification accent color. Desktop notifications are not yet wired, so the
 * token carries no value — it exists only to satisfy the common [NotificationAppearance] contract.
 */
actual class NotificationAccentColor
