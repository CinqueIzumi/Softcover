- [OfflineSync mocking pattern in BooksRepositoryImplTest](feedback_offline_sync_mocking_pattern.md) — relaxed offlineSync mock silences initializeBooks; every refreshUserBooks test needs a pass-through drainAndReconcile stub
- [Kotest reference equality: always parenthesise ===](feedback_kotest_reference_equality.md) — `a === b shouldBe true` is wrong (=== < infix precedence); write `(a === b) shouldBe true`
- [Coroutine-safe tests: share ONE TestDispatcher/scheduler](feedback_coroutine_safe_tests.md) — mismatched schedulers (e.g. Room `queryContext` dispatcher ≠ `runTest`'s) hang the worker at 0% CPU or pass by luck; use one dispatcher field everywhere + `runTest(testDispatcher)` + `setMain`/`resetMain`; verify with a bounded run
- [jvmTest task returns NO-SOURCE](project_jvm_test_task_name.md) — androidHostTest source sets run under `testAndroidHostTest`, not `jvmTest`; confirmed in :core:domain and :feature:book_detail
- [StateFlow transient-reset testing](feedback_stateflow_transient_reset_testing.md) — testing a reset-then-recompute double `.update{}` needs 3 DISTINCT values (baseline/reset/final) or StateFlow conflation hides the transient emission
- [Apollo taggable_counts query mocking](feedback_apollo_taggable_counts_mocking.md) — mockkObject(X.Data.Taggable_count.Book.Companion) pattern for GetBooksByMoodTagQuery/GetBooksByGenreTagQuery; hasMore uses RAW row count not deduped count
- [DAO-backed LocalDataSource Flow tests](feedback_flow_dao_local_datasource_pattern.md) — plain `flowOf` + turbine `.test{}` is enough for a DAO passthrough Flow; no TestDispatcher needed unless a real dispatcher is involved
- [Third-party import order convention](feedback_import_order_convention.md) — ASCIIbetical full-path sort: io.* → kotlin.* → kotlinx.* → nl.rhaydus.* → org.junit.*; fix on touch per on-touch style policy
- [MockK default-param stub gotcha](feedback_mockk_default_param_stub_gotcha.md) — a coEvery stub that omits a trailing default-valued param implicitly pins it to that default, not "any"; new tests using a non-default value need their own explicit-matcher coEvery
- [Optional-field threading test pattern](feedback_optional_field_thread_mockk_pattern.md) — mirror each layer's existing coEvery/coVerify idiom per-layer (datasource/repo/usecase/offline-sync) when threading a new optional param through a write path; check a "replay" counterpart actually has tests before assuming coverage exists
- [Stale import order in core/book test files](feedback_stale_project_import_order_in_core_book.md) — 5 files had org.junit before nl.rhaydus; fixed on touch during actionAt-backdating pass (2026-07-22)
- [Apollo pagination adds required query params](feedback_apollo_pagination_required_params.md) — new `limit`/`offset` GraphQL vars break old bare `XQuery(userId=...)` test call sites; fix minimally alongside new pagination tests, even in a tightly-scoped brief
