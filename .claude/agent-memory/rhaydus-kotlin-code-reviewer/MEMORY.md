- [TOAD one-shot init convention](architecture_toad_init_side_effects.md) — one-shot ScreenModel init work should go through a Collector.onLaunch or dispatch(Action), not a raw `dependencies.launch{}` in `init`
- [Import grouping: guide-enforced, not ktlint](project_import_order_debt.md) — guide mandates Android→third-party→`nl.rhaydus.*`; ktlint does NOT catch it (standard ruleset disabled); fix deviations wherever found, not just on touched files
- [One-type-per-file: colocated support data class](style_one_type_per_file_colocated_support_class.md) — a local `private data class` in a screen/component file is a blocking ktlint violation even in an otherwise-clean rewrite; check every new data class/enum for its own file; ALSO check detekt's MatchingDeclarationName (1-type-but-misnamed-file), which ktlint's rule doesn't cover
- [Full-height sheet pinning is safe](architecture_full_height_sheet_pinning.md) — verified via M3 source: pinned rows + `weight(1f)+verticalScroll` body inside `AdaptiveModalSheet` correctly fills height and doesn't crash, on both SHEET and PANEL forms; don't re-litigate
- [Detekt gate scope: main source sets only](project_detekt_gate_scope.md) — styleCheck/check only gate detektAndroidMain/detektJvmMain/detektMain; detektAndroidHostTest findings are informational, not blockers
