mutation UpdateBookStatus(
    $id: Int!,
    $object: UserBookUpdateInput!,
) {
    update_user_book(
        object: $object,
        id: $id,
    ) {
        error

        user_book {
            id

            book {
                ...BookFragment
            }
        }
    }
}
