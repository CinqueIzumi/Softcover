fragment UserBookReadFragment on user_book_reads{
    id
    progress
    progress_pages
    started_at
    finished_at

    user_book {
        ...UserBookFragment

        __typename
    }

    edition {
        ...EditionFragment

        __typename
    }

    __typename
}